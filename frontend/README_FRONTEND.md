# Frontend — Task Tracker Lite

## Setup
1. Node 18+ recommended.
2. Install:
   npm install
3. Run:
   npm run dev
4. The frontend expects the backend API at http://localhost:5000/api by default.
5. Tailwind is included (postcss + tailwindcss).
